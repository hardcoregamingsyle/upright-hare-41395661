generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

model Quote {
  id        Int      @id @default(autoincrement())
  text      String
  source    String?
  createdAt DateTime @default(now())
}
